# 🧭 FitTrack — UI Wireframes & Screen Descriptions

## Общее описание
Этот документ описывает пользовательские интерфейсы (UI) и логику взаимодействия для FitTrack.  
Используется для проектирования компонентов Angular, маршрутов (`RouterModule`) и UX-потоков.  
Каждый экран содержит назначение, элементы управления и связи с API.

---

## 🏠 Главный экран (Home Screen)

**Назначение:**  
Главная точка входа в приложение. Отображает дневной прогресс и предоставляет быстрый доступ к основным функциям.

**Элементы:**
- Приветствие пользователя (`{{user.name}}`)
- Прогресс воды (progress-bar + цель в мл)
- Кольцевая диаграмма калорий (сегменты: белки, жиры, углеводы)
- Кнопки:
  - ➕ «Добавить еду»
  - 💧 «Добавить воду»
- Кнопка ⚙️ «Настройки»
- Кнопка 📈 «Статистика»

**API:**
- `GET /api/food?date=today`
- `GET /api/water/today`
- `GET /api/vitamins/balance`

---

## 💧 Учёт воды (Water Tracker)

**Назначение:**  
Быстрый учёт выпитой жидкости.

**Компонент:** `water-tracker.component.ts`

**Элементы:**
- Текущий прогресс (progress-bar + % от дневной цели)
- Кнопки:  
  - `+200 мл`, `+500 мл`, `+1000 мл`
- Кнопка 🔄 «Сбросить день»
- Список записей (по времени добавления)
- Кнопка “Синхронизировать” при возвращении онлайна

**API:**
- `GET /api/water/today`
- `POST /api/water/add`
- `DELETE /api/water/{id}`

---

## 🥗 Учёт питания (Food Diary)

**Назначение:**  
Фиксировать приёмы пищи и рассчитывать калории/витамины.

**Компонент:** `food-diary.component.ts`

**Элементы:**
- Таблица:
  - Название блюда
  - Калории
  - БЖУ (белки, жиры, углеводы)
- Кнопка ➕ “Добавить”
- Модальное окно «Новый приём пищи»
  - Поля ввода: название, калории, белки, жиры, углеводы, JSON витаминов
  - Кнопка “Сохранить”
- Итог за день: “Всего калорий: 1820”

**API:**
- `GET /api/food`
- `POST /api/food`
- `DELETE /api/food/{id}`

---

## 🌿 Баланс витаминов (Vitamin Balance)

**Назначение:**  
Отображение уровня витаминов и рекомендации.

**Компонент:** `vitamin-balance.component.ts`

**Элементы:**
- Заголовок: “Баланс витаминов”
- Круговые или столбчатые диаграммы по каждому витамину
- Цветовая индикация:
  - 🟢 норма (>= 90%)
  - 🟡 лёгкий дефицит
  - 🔴 критический дефицит
- Кнопка 🔍 “Рекомендации по питанию”
- Карточки продуктов, богатых нужным витамином

**API:**
- `GET /api/vitamins/balance`
- `POST /api/vitamins/recommendations`

---

## ⏰ Напоминания (Reminders)

**Назначение:**  
Управление push-уведомлениями и расписанием напоминаний.

**Компонент:** `reminders.component.ts`

**Элементы:**
- Таблица:
  - Тип (вода / еда / витамин)
  - Время
  - Интервал (мин)
  - Статус (вкл./выкл.)
- Кнопка ➕ “Добавить напоминание”
- Модальное окно для создания/редактирования
  - Выбор типа (`select`)
  - Время (`timepicker`)
  - Интервал (`number input`)
  - Переключатель активности (`mat-slide-toggle`)

**API:**
- `GET /api/reminders`
- `POST /api/reminders`
- `PATCH /api/reminders/{id}/toggle`

---

## 📊 Статистика (Stats Dashboard)

**Назначение:**  
Отображение недельных и месячных тенденций потребления.

**Компонент:** `stats.component.ts`

**Элементы:**
- Вкладки:
  - Калории
  - Вода
  - Витамины
- Графики:
  - `LineChart` для воды
  - `BarChart` для калорий
  - `RadarChart` для витаминов
- Кнопка 📅 “Выбрать неделю”
- Сводная таблица
  - Среднее значение
  - Минимум / максимум
  - Прогресс к цели

**API:**
- `GET /api/stats/weekly`
- `GET /api/vitamins/balance`

---

## ⚙️ Настройки (Settings)

**Назначение:**  
Настройка целей и интерфейса пользователя.

**Компонент:** `settings.component.ts`

**Элементы:**
- Поля:
  - Цель по воде (в мл)
  - Цель по калориям (в ккал)
  - Тема (светлая/тёмная)
  - Язык интерфейса
- Кнопка 💾 “Сохранить”
- Кнопка 🔙 “Назад”

**API:**
- `PATCH /api/users/profile`

---

## 💡 Диалоги и состояния

**Офлайн-режим**
- Отображение баннера: “Вы офлайн — данные сохраняются локально.”
- При возвращении онлайн → авто-синхронизация.

**Загрузка**
- Skeleton-placeholder или `mat-progress-bar`.

**Ошибки**
- Snackbar с сообщениями: “Ошибка сети”, “Неверный логин”, “Нет данных”.

---

## 🧭 Навигация (Routing Map)

| Путь | Компонент | Назначение |
|------|------------|-------------|
| `/` | HomeComponent | Главная страница |
| `/water` | WaterTrackerComponent | Учёт воды |
| `/food` | FoodDiaryComponent | Учёт питания |
| `/vitamins` | VitaminBalanceComponent | Баланс витаминов |
| `/reminders` | RemindersComponent | Напоминания |
| `/stats` | StatsComponent | Статистика |
| `/settings` | SettingsComponent | Настройки |

---

## 🎨 Стиль и UI-гид
- Цветовая палитра:  
  - Основной (`primary`): `#00796b`  
  - Акцент (`accent`): `#4db6ac`  
  - Фон (`background`): `#f5f5f5`
- Типографика: Roboto 400/500
- Отступы: `8–16px`
- Радиус элементов: `12px`
- Иконки: `Material Icons`
- Анимации: `Angular Animations` (slide-fade, scale-in)

---

**Ответственный:** FitTrack Design System  
**Последнее обновление:** 29 октября 2025  
**Версия:** 1.0
